import 'package:flutter/material.dart';

class CollectShelfPage extends StatelessWidget {
  const CollectShelfPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("收藏"),
    );
  }
}
